# app
apiVersion: v1
kind: Service
metadata:
  labels:
    name: green-ops-app
  name: green-ops-app
  namespace: green-ops
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      name: green-ops-app
  selector:
    app: green-ops-app
